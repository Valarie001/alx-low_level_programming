#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */

int main (void)
{
	printf("size of a char: 5zu byte(s)\n", sizeof(char)):
	printf("size of an int: %zu byte(s)\n", sizeof(int));
	return(0)
}
